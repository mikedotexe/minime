{
  "name": "llava-worker",
  "version": "0.1.0",
  "type": "module",
  "description": "Streaming proxy that relays Ollama LLaVA output as Server-Sent Events.",
  "scripts": {
    "start": "node llava_worker.mjs"
  }
}
{
  "dependencies": {
    "ws": "^8.18.3"
  }
}
