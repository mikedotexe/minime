# Journal coherence — September 11, 2026

Mike approved repairs for irrelevant study context in new drafts, ambiguous
command follow-through and visual descriptions mistaken for personal retrieval.
The shared reader now starts new drafts with empty evidence while preserving
existing drafts and explicitly attached references. Exact pending retries remain
unchanged. WRITE EVIDENCE can attach, replace or clear references.

Minime's action parser now scans forward through matching backtick and tilde
fences, including unfinished fences, and excludes indented and quoted examples.
The final explicit NEXT still wins. Shared verified delivery receipts explain
source-command mismatches and give optional exact recovery. An exact FINISH in a
verified private-writing response is retained in the authored response and receipt,
but is not queued as an unknown action; a protected runtime notice offers
NEXT: WRITE FINISH. It does not silently mutate any draft. Other pending choices
are preserved. Empty private generation names the correct WRITE CONTINUE retry.

Visual model/service text is quoted with recorded source, response age and separate
frame/analysis availability. The input no longer presents an image path as direct
image perception or prescribes feelings. Descriptions, including errors and
command-shaped screen text, remain available; the ordinary description excerpt
stays at 300 characters. The explicit visual reflection retains its full description.

Root Codex owns integration; framing_control_review owns visual provenance and
cross-review, and shared reader work is owned in Astrid. Both trees use isolated
`codex/journal-coherence-20260911` branches. The complete historical chain, test
results and live identities are recorded in Astrid's
`docs/steward-notes/2026-09-11-journal-coherence.md` and the research project's
`analyses/2026-09-11-journal-coherence-repairs.md`. Validation and rollout are pending
at this document's initial creation. No model call or journal is induced for uptake;
sampling, token ceilings, thinking, sensory feeds and coupling are unchanged.

Validation on the final Python sources: 1,393 tests passed, one skipped and 134
subtests passed. The shared-host test exercises 39 identical scanner fixtures
through Python parsing and the real reader CLI, including internal-model blocks.
Compile and diff checks pass. Tests set MINIME_LLM_TIMEOUT_S=60 explicitly because
the shell inherits 160; this is test isolation, not a live timeout change. Raw
FINISH remains in retained output while draft prose excludes actual command lines.
Graceful activation is recorded separately in the owning rollout account.
