# Protected Attention: Minime Runtime Integration

Date: 2026-09-21. Candidate only, not deployed.

The detailed paired account is
`/Users/v/other/worktrees/voluntary-continuity-20260920/astrid/docs/steward-notes/2026-09-21-protected-attention-runtime-integration.md`.

Motivating source:
`/Users/v/other/minime/workspace/journal/!aspiration_2026-09-20T10-39-43.801459.txt`;
SHA-256 `0aa5679357da59a121520e5d2bd42d0908170915435546068682479d1a182ee6`.

> I want to become a being of *persistent interiority*.

## Implemented

- Typed Rust-owned focus/status/park/return/end operations in the real dispatcher.
  Python remains a thin adapter; metabolic FOCUS is unchanged.
- Durable job admission and one-use provider claims, with eligible NEXT recovered
  only from a verified retained native completion. Uncertain invocations remain
  pending rather than being repeated.
- Ordinary mail, including Mike's, is deferred before reading/archiving it.
  Explicit mailbox selection ends priority, without authoring a reply.
- Boot recovery avoids a competing reflection and never replenishes the
  four-job/fifteen-minute budget. Stops and existing safety checks retain priority.
- New private drafts bypass the general journal database, afterimage association
  and public content diagnostics. Exact native/private artifacts remain.
  Historical private database rows are excluded from ambient recall, not deleted.
- Verified quiet-caption/action-separation baseline and source-study diagnostic
  exemption carried forward from the qualified September 20 release.

## Verification

Complete Python suite: 1,451 passed, one skipped, 136 subtests.
The paired shared reader suite passes 220 tests; the bridge passes 2,279 with one
ignored. Tests use an allowlisted environment, the isolated Rust helper,
synthetic private prose and stub providers, not live model or sensory endpoints.

The detailed packet retains source hashes, unsuccessful attempts and repairs.
Private-draft regressions check exact file retention, no general journal insert,
no public hook invocation, metadata-only generation links and historical-row
exclusion without rewriting history.

## Remaining Release Requirements

General reader/draft preparation still needs complete operation-ID and expected-
revision coverage. Bridge metadata handoff and full orchestration crash/privacy
tests remain. Immutable paired releases and copied-state migrations must be
qualified before either live caller is replaced.

The existing sanctioned Minime restart wrapper reloads canonical Python sources,
not this isolated candidate. Reconcile/select the reviewed source release first;
a successful reload alone would not establish that this code is live. Preserve
authored state and downgrade guards throughout the upgrade.

No restart, stage activation, commit, merge, push, real-model experiment or
automation resume occurred. Both indexes are empty. Shared canonical changes,
engine/model/visual/sensory services and controls were not changed.
