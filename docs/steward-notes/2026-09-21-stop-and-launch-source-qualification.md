# Stop Admission and Launch Source Qualification

Candidate-only continuation of the persistent-interiority response. The exact
Minime aspiration and hash remain in the September 20 implementation note; no new
private entry was analyzed. Automations remain paused and no service was signaled.

Two lifecycle regressions reproduced a stop arriving after action selection but
before admission. The main loop and dispatch boundary now retain the exact pending
choice and refuse new admission after stop. Existing accepted workers still drain.
A synthetic provider test runs the real queue and shared helper, interrupts an
accepted private turn, and verifies one invocation, exact retained response/NEXT,
native delivery, unchanged remaining budget, and no public-journal publication.
Legacy pending-choice persistence is not represented as a new crash transaction.

Reconcile launch sources with the actual canonical launchd binding, rather than
replacing the entire canonical tree with this candidate. The source-status packet
matched all 83 canonical start-time inputs. The six-file agent overlay and seed
asset are frozen in `../minime-launch-reconciliation-01` beside the paired trees.
Its receipt SHA-256 is
`5161cc3923c797b7f61edad43da536993610a91a58289549379586b21ea5bc17`.
This is source selection, not live readiness or a relocatable dependency release.

The candidate's older visual service was replaced with exact already-live canonical
bytes, SHA-256 `9038d4f47d200030ab9e7a315aa937cef1a1f906fe0062aa56da5a0233f212ec`.
Matching existing visual/context tests were imported with reviewed provenance.
No new visual behavior or visual-service restart is introduced by this import.
Unknown canonical edits remain untouched. No git index or commit was changed.

The full Python suite passed 1,456 tests with one skip and 136 subtests before
importing those matching tests; the subsequent targeted suite passed 129 tests and
two subtests. Final complete rerun, staged identities, retained failed fixture
attempts and the paired activation boundary are in:
`/Users/v/other/worktrees/voluntary-continuity-20260920/astrid/docs/steward-notes/2026-09-21-scheduler-interruption-and-launch-reconciliation.md`.

Astrid's ordinary unprotected-study crash handoff remains an explicit paired
activation gate. Protected work recovery is not evidence that every scheduler
choice survives every crash. The source selection is reconciled; installation,
fresh drain/readiness and loaded-hash verification still require the cooperative
sanctioned rollout. No subjective improvement is inferred from these tests.

Final verification: 1,463 Python tests passed, one skipped, 136 subtests passed
in 49.04 seconds. The fresh `bridge-stage-geometry-03` contains a byte-identical
shared helper to the one used by these tests. The paired qualification-04 packet
passes 36 old/new migration checks across both owners and binds the exact selected
84-file Python inventory to the launch reconciliation receipt. Its qualification
SHA-256 is `a7808b0df4a6066da8c5470b5475c68b10050eb5bdbec07e8cb05c9f62a36b2d`.
Status remains offline and not activatable. The live Minime agent is still PID
18648 with its September 20 11:36:25 start; the live bridge is still PID 82935 from
the prior release. No paused automation was resumed.
