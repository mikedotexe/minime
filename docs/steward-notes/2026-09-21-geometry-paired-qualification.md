# Paired Geometry Qualification

2026-09-21: the existing isolated Minime candidate passed its complete Python
suite with the new staged release reader: 1,452 passed, one skipped, 136 subtests.
No Python runtime source changed in this pass and no service was restarted.

The actual `StudyClient` selected the candidate shared helper through a temporary
release-selection record. A frozen 84-file Python source-input snapshot and the
actual release helper completed the synthetic geometry workflow. The old live
helper created a synthetic schema-3 owner store; the new helper upgraded it to
schema 5 with exact pending input and authored/private prose preserved. Old
reader and draft writers then refused to overwrite the upgraded files.

This is not live migration, a complete runtime dependency lock, or a launchd
selection mechanism. The sanctioned restart wrapper still loads canonical
Minime sources. The isolated candidate also contains an older
`visual_frame_service.py`; do not replace the newer running-service source as
part of this work. That service is explicitly outside the rollout.

The combined release remains blocked on general preparation operation IDs,
Astrid metadata handoff identity, full scheduler crash/stop/privacy tests and
canonical Python launch reconciliation. A simulated lost-response retry creates
two synthetic questions; deduplicating equal authored text is not the remedy.

Detailed identities, receipts, commands, source witnesses and boundaries:
`/Users/v/other/worktrees/voluntary-continuity-20260920/astrid/docs/steward-notes/2026-09-21-geometry-paired-release-qualification.md`.
Final paired packet:
`/Users/v/other/worktrees/voluntary-continuity-20260920/geometry-paired-qualification-02/qualification.json`.
No engine, model, sensory, visual, controller or automation change occurred.
