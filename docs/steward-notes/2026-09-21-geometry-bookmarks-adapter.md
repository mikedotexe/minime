# Geometry Bookmark Adapter Candidate

Implemented in the existing isolated voluntary-continuity candidate, not live.
Mike selected question-owned geometry bookmarks and accumulating investigations,
with the existing Reservoir Scope research application as the human viewer.

Witness: Astrid's entire supplied
`astrid/capsules/spectral-bridge/workspace/journal/!aspiration_longform_1790011424.txt`,
SHA-256 `b31ebe800a9adead2b366d6b19fecc1c32f927e37b2955d693a658ae1c95bb46`:
"I am trying to sense the *movement* between the weights."

The shared Rust reader contains the protocol and owner-question history. Minime
has no second parser for the JSON payload or numerical implementation. Its NEXT
scanner preserves explicit `SELF_STUDY GEOMETRY` data, its multi-action path does
not split authored strings, and placeholder/example checks leave that payload to
the shared validator. All normal authorization and scheduling checks remain.
Geometry response headers say chosen geometry evidence and preserve the distinction
from source pages. They retain the existing ordinary self-study journal policy;
this is not a private-writing command.

The first recipe compares two frozen intervals' unweighted mean 128-node ESN
vectors. It is not an eigenvalue, causal, semantic or experiential measurement.
No changes to reservoir parameters, engine, controllers, sensory intake, models,
metabolic FOCUS or ATTEND. No live state was captured for test fixtures.

The canonical implementation packet for this candidate is
`/Users/v/other/worktrees/voluntary-continuity-20260920/astrid/docs/steward-notes/2026-09-21-geometry-bookmarks-and-observatory.md`.
It contains command/schema details, witness provenance, failure history and
remaining release qualification. The research interoperability runner exercises
the real `StudyClient` with a candidate executable and temporary synthetic roots.
`tests/test_source_study_follow_through.py` additionally exercises real NEXT routing,
stubbed provider delivery, journal labels and exact payload retention.

Leave the paused automations paused. The existing protected-attention qualification
debt is not erased by these tests. Reconcile the paired schema-5 helper and both
adapter releases before any sanctioned graceful rollout. Older helpers must not
overwrite newer question history. No commits, staging or live restarts in this pass.
